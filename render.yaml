services:
  - type: web
    name: whatsapp-bot
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn bot:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
